{
  "name": "percentage-ranges",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check": "tsc --noEmit --skipLibCheck",
    "lint": "eslint . --fix"
  },
  "dependencies": {
    "common": "workspace:*"
  }
}
